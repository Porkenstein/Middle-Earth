decisions = {
	abdicate_throne_elf = {
		potential = {
			is_ruler = yes
			NOT = { has_character_flag = wants_to_abdicate }
			current_heir = { dynasty = ROOT }
			age = 100
			religion_group = religion_group_eldar
			# Traits checks
			NOT = { trait = lunatic }
			NOT = { trait = possessed }
			NOT = { trait = ambitious }
			NOT = { trait = envious }
			NOT = { trait = greedy }
			NOT = { trait = incapable }
			NOT = { trait = oath_of_feanor }
			OR = {
				ai = no
				NOT = { has_character_flag = plot_armor } #emotional plot armor
			}
		}
		allow = {
			war = no
			prisoner = no
			# Age checks
			OR = {
				AND = {
					OR = {
						trait = amateurish_plotter
						trait = naive_appeaser
						trait = indulgent_wastrel
						trait = misguided_warrior
					}
					age = 200
					trait = elf
				}
				AND = {
					trait = stressed
					age = 200
					trait = elf
				}
				AND = {
					trait = depressed
					age = 100
					trait = elf
				}
                AND = {
					trait = maimed
					age = 100
					trait = elf
				}
			}
			# Costs
			prestige = 250
			piety = 100
		}
		effect = {
			prestige = -250
			piety = -100
			set_character_flag = wants_to_abdicate
			current_heir = {
				character_event = {
					id = ABDICATE.001
					days = 3
					random = 3
					tooltip = "abd_ask"
				}
			}
		}
	}
	abdicate_throne_dwarves = {
		potential = {
			ai = no
			is_ruler = yes
			NOT = { has_character_flag = wants_to_abdicate }
			current_heir = { dynasty = ROOT }
			culture_group = culture_group_dwarves
			age = 100
			# Traits checks
			NOT = { trait = lunatic }
			NOT = { trait = possessed }
			NOT = { trait = imbecile }
			NOT = { trait = cruel }
			NOT = { trait = ambitious }
			NOT = { trait = envious }
			NOT = { trait = greedy }
			NOT = { trait = incapable }
		}
		allow = {
			war = no
			prisoner = no
			# Age checks
			OR = {
				AND = {
					trait = dwarven
					age = 300
				}
				AND = {
					trait = stressed
					trait = dwarven
					age = 200
				}
				AND = {
					trait = dwarven
					trait = depressed
					age = 100
				}
			}
			# Costs
			prestige = 250
			piety = 100
		}
		effect = {
			prestige = -250
			piety = -100
			set_character_flag = wants_to_abdicate
			current_heir = {
				character_event = {
					id = ABDICATE.001
					days = 3
					random = 3
					tooltip = "abd_ask"
				}
			}
		}
	}
}
