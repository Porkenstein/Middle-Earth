decisions = {
	sauron_restore_form = {
		is_high_prio = no
		potential = {
			ai = no
			age = 1
			prisoner = no
			trait = spiritform
		}
		allow = {
			has_artifact = artifact_ring
			trait = spiritform
		}
		effect = {
			character_event = { id = sauronrestoreform.0 }
		}
	}
}
