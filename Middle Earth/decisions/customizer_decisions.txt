dynasty_decisions = {
	blood_of_aldarion = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.1 }
		}}
	}

dynasty_decisions = {
	blood_of_anarion = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.2 }
		}}
	}

dynasty_decisions = {
	blood_of_durin = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.3 }
		}}
	}


dynasty_decisions = {
	blood_of_arpharazon = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.4 }
		}}
	}

dynasty_decisions = {
	blood_of_elwe = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.5 }
		}}
	}

dynasty_decisions = {
	blood_of_feanor = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.6 }
		}}
	}

dynasty_decisions = {
	blood_of_finarfin = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.7 }
		}}
	}

dynasty_decisions = {
	blood_of_fingolfin = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.8 }
		}}
	}

dynasty_decisions = {
	blood_of_galadhon = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.9 }
		}}
	}

dynasty_decisions = {
	blood_of_dale = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.10 }
		}}
	}

dynasty_decisions = {
	blood_of_hurin = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.11 }
		}}
	}

dynasty_decisions = {
	blood_of_imrazor = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.12 }
		}}
	}

dynasty_decisions = {
	blood_of_isildur = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.13 }
		}}
	}

dynasty_decisions = {
	blood_of_nowe = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.14 }
		}}
	}

dynasty_decisions = {
	blood_of_olwe = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.15 }
		}}
	}

dynasty_decisions = {
	blood_of_oropher = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.16 }
		}}
	}

dynasty_decisions = {
	blood_of_thorondor = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.17 }
		}}
	}

dynasty_decisions = {
	blood_of_valandil = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.18 }
		}}
	}

dynasty_decisions = {
	blood_of_vidugavia = {
		only_playable = yes
		ai = no
		potential = {
			ai = no
			has_character_flag = dynasty_decisions_open
		}
		allow = {
			is_adult = yes
			prisoner = no
			NOT = { has_character_modifier = joined_dynasty }
			custom_tooltip = {
				text = is_not_busy_trigger_tooltip
				hidden_tooltip = { NOT = { has_character_flag = do_not_disturb } }
			}
		}
		effect = {
			character_event = { id = customizer_houses.19 }
		}}
	}
