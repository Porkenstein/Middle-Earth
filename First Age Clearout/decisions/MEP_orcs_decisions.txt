decisions = {
	## Decision for an ork vassal to claim the crown of his liege ##
	claim_throne_orcs = {
		potential = {
			#ai = no
			liege = {
				is_orc_culture = yes
				NOT = { has_character_flag = orcfight1 }
				NOT = { character = ROOT }
			}
			is_orc_culture = yes
			NOT = { has_character_flag = orcfight1 }
			NOT = { is_female = yes }
			NOT = { trait = incapable }
			NOT = { trait = blinded }
			NOT = { trait = craven }
		}
		allow = {
			war = no
			prisoner = no
			liege = {
				NOT = { martial = ROOT }
			}
			NOT = { trait = weak_orc }
			prestige = 500
		}
		effect = {
			character_event = { id = orcfights.0 }
			set_character_flag = orcfight1
			liege = { set_character_flag = orcfight1 }
		}
	}
	
	## Decision to draft a snaga mob to war. This will annoy your slave pit masters. ##
	draft_snaga_mob = {
		is_high_prio = yes
		potential = {
			is_orc_culture = yes
		}
		allow = {
			war = yes
			prisoner = no
			wealth = 100
		}
		effect = {
			prestige = -100
			wealth = -100
			add_character_modifier = { name = drafted_slaves duration = 365 }
			capital_scope = {
				PREV = {
					spawn_unit = {
						owner = ROOT
						leader = none
						province = PREV
						home = PREV
						troops = {
							archers = { 250 250 }
							light_infantry = { 1250 1250 }
						}
						attrition = 1.0
						disband_on_peace = no
						maintenance_multiplier = 0.1
					}
				}
			}
		}
		ai_will_do = {
			factor = 1

			modifier = {
				factor = 0
				OR = {
					NOT = { any_war = { war_score = -20 } }
					NOT = { is_primary_war_defender = yes }
				}
				
			}
		}
	}
}
